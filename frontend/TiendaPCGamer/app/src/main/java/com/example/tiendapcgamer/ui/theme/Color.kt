package com.example.tiendapcgamer.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales oscuros
val CyberpunkPrimary = Color(0xFF1E40AF)         // Azul vibrante serio (antes 0xFF0800FF)
val CyberpunkSecondary = Color(0xFF6B21A8)       // Morado profundo y elegante (antes 0xFF9C27B0)
val CyberpunkPrimaryDark = Color(0xFF0F172A)     // Azul muy oscuro, casi negro (antes 0xFF001756)
val CyberpunkError = Color(0xFFEF4444)           // Rojo vibrante y claro (antes 0xFFFF5252)

// Colores claros para botones y resaltados
val CyberpunkPrimaryLight = Color(0xFF3B82F6)    // Azul claro brillante y profesional (antes 0xFF4C6FFF)
val CyberpunkSecondaryLight = Color(0xFFA78BFA)  // Morado claro moderno y sobrio (antes 0xFFD05CE3)

// Fondo y texto
val BlackBackground = Color(0xFF111827)          // Fondo negro con tintes azulados (antes 0xFF121212)
val DarkSurface = Color(0xFF1F2937)              // Superficies oscuras gris-azuladas (antes 0xFF1E1E1E)
val LightGrayText = Color(0xFFD1D5DB)            // Texto gris claro para buena legibilidad (antes 0xFFE0E0E0)
